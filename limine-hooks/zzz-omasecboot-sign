#!/bin/bash
set -euo pipefail

# The dispatcher validates lifecycle ownership and either retains the parent's
# FD 200 lock or acquires the shared lock before recording required repair.
exec @BINDIR@/omasecboot --quiet hook post
